# Agent Skill 管理约定

本项目由用户通过自然语言管理 Skill。不要要求用户手工编辑 `SKILL.md`、`skill.json`、测试样例、slug、版本号、安装目录或下载包。用户只需说明希望新增、修改、查询、测试、同步、安装、卸载、删除或恢复什么，Agent 负责完成余下工作。

## 数据边界

- `skills/` 是唯一中央源码，一项 Skill 对应一个独立目录。
- 每个目录必须包含 `SKILL.md`、`skill.json` 和 `tests/prompts.json`。
- `SKILL.md` 遵循 Agent Skills 结构：必需 `name`、`description`，可附带 `scripts/`、`references/`、`assets/` 和 `agents/openai.yaml`。
- `.agents/skills/` 是 Codex 当前项目的安装目标，只能由安装/同步流程更新，不直接编辑。
- `data/generated/skills.json` 与 `public/skills/` 是生成结果，不作为手工数据源。
- 不再保留旧案例库与 `solutions/` 兼容层，所有正式内容统一进入 `skills/`。
- `data/generated/skills.json` 是一次发布的只读快照。公开站点和 `/api/v1/*` 只读取该快照，不直接扫描或修改中央源码。
- `/api/v1/*` 是公开只读与下载接口；`/api/local/*` 是维护者本地接口，仅允许服务绑定回环地址时启用。
- 云端不部署 Agent，不提供在线编辑表单，也不把 `.agents/skills/` 当作公开状态。访客只能浏览公开 Skill、查看说明和下载。

## 自然语言管理

### 新增

1. 根据需求生成稳定、简短的英文 Skill id，并在 `skills/<id>/` 创建独立目录。
2. 完成可用的 `SKILL.md`，触发描述必须明确何时使用和何时不使用。
3. 补充版本、分类、标签、兼容环境、运行要求及正向/反向触发测试样例。
4. 用户明确要求开发时，完成脚本、参考资料和资源，不只建立空壳。

### 查询

优先扫描 `skills/*/skill.json`、`SKILL.md` 和生成索引，按名称、描述、标签、分类、测试与安装状态回答，不让用户自行翻目录。

### 修改

只修改 `skills/<id>/` 中的中央源码。行为或内容变化时按语义化版本更新 `skill.json`，同步 README、测试样例和必要资源；不要直接修改 `.agents/skills/` 中的副本。

### 测试

运行 Skill 测试与安全扫描。结构测试通过不等同于完整运行验证；涉及外部输入、网络或脚本时，使用代表性样例完成与风险匹配的运行测试，并如实说明边界。

### 使用与下载统计

- 用户通过平台下载 Skill ZIP 时，由本地服务自动记录一次下载；HEAD 请求、分段请求和测试探测不计数。
- Agent 真正调用某个 Skill 并成功交付结果后，执行一次 `skills:record-use -- <id>`。测试、安装、同步、失败或未完成的调用不计入使用次数。
- 统计保存在 `SKILLDOCK_STATS_PATH` 指定的服务端持久化文件（本地默认 `data/local/skill-stats.json`），不进入中央源码、下载包或 Git。
- 查询总次数及单个 Skill 次数时使用 `skills:stats` 或本地统计接口，不让用户手工维护计数。
- 统计同时维护累计值和按日聚合值，默认使用 `Asia/Shanghai` 归档；页面通过公开统计接口约每 15 秒刷新，只使用站内第一方轻量事件，不接入第三方追踪脚本。
- 页面浏览和 Skill 点击使用站内第一方事件接口，只记录公开路径、Skill id、事件类型和时间。接口必须保持同源校验、事件去重、路径白名单和频率限制，不持久化 IP 或用户内容。

### 安装与同步

- 安装前必须测试通过。
- 安装使用中央源码写入 `.agents/skills/<id>/`。
- 同步只更新已经安装的 Skill；未安装的 Skill 需要明确执行安装。
- 修改中央源码后检查内容指纹，确保目标状态由“待更新”恢复为“已同步”。

### 公开发布

- 只有维护者可以通过本地 Agent 修改 `skills/`。访客不需要登录，但只有读取和下载权限。
- 发布前依次完成测试、生成版本化 ZIP、更新发布快照和站点构建；不得让云端服务直接修改中央源码。
- 公开下载必须经过 `/api/v1/skills/<id>/download`，由接口记录一次下载后跳转到版本化 ZIP。页面不要直接链接对象存储地址。
- 版本化 ZIP 不覆盖旧版本。行为或内容变化时先更新语义化版本，再发布新包。
- 云服务器绑定到非回环地址时必须确认 `/api/local/*` 返回 404，公开 API 不泄露源码目录、安装目标或内容指纹。

### 删除与恢复

- 默认删除是可恢复删除：精确确认目标后移动到 `skills/_trash/`，不要直接永久清除。
- 卸载只移除安装副本，不删除中央源码。
- 只有用户明确要求永久删除时才清理回收站中的准确目标。

## 每次变更后的收尾

1. 运行 `skills:test`，确保结构、触发样例和安全检查通过。
2. 根据用户意图执行安装或同步，然后运行 `skills:sync` 更新索引、ZIP 与 SHA-256。
3. 运行与改动风险匹配的类型检查、代码检查和构建。
4. 若本地站点正在运行，确认首页、Skill 详情、公开统计和下载跳转正常。
5. 验证公开接口只返回发布快照，并确认仅本地维护接口在公网模式不可用。
6. 若本次任务确实使用了某个 Skill 完成交付，最后记录一次使用；不要给测试或安装行为重复计数。
7. 向用户说明 Skill 的版本、测试结果、安装/同步状态；不要把文件维护工作转交给用户。
